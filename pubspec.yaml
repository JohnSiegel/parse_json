name: json
description: Type safe JSON serialization/deserialization for Dart with no code generation.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  
dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.5
